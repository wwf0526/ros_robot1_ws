qp_mpc_node:
  ros__parameters:
    # 当前执行频率；输出为零点坐标系下的六电机绝对位置目标。
    control_rate_hz: 5.0
    max_delta_deg: 1.0
    default_speed_rad_s: 0.3
    motor_limit_deg: 180.0

    # B 矩阵仍需通过实机数据辨识；在完成辨识前不得进入无人值守实机闭环。
